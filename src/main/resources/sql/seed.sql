USE echo_community;

DELETE FROM moment WHERE user_id IN (SELECT id FROM user WHERE username IN ('admin', 'zhangsan', 'lisi'));
DELETE FROM interview WHERE user_id IN (SELECT id FROM user WHERE username IN ('admin', 'zhangsan', 'lisi'));
DELETE FROM column_article WHERE user_id IN (SELECT id FROM user WHERE username IN ('admin', 'zhangsan', 'lisi'));
DELETE FROM post WHERE user_id IN (SELECT id FROM user WHERE username IN ('admin', 'zhangsan', 'lisi'));
DELETE FROM user WHERE username IN ('admin', 'zhangsan', 'lisi');

-- ═══ 用户（注册时间拉早到 2025-08） ═══
INSERT INTO user (username, password, email, role, status, created_at) VALUES
('admin',    '$2b$10$yUDFN1l8B7qLhCuxsoz2tOwX2bwTqg0WyPbAcgdMT1rg26VhmHQrG', 'admin@echo.com',    'ADMIN', 0, '2025-08-15 10:00:00'),
('zhangsan', '$2b$10$J2QgizK4xegmtQkb6GklL.UrF/6mm5awsmNdytzf2Qqyl89NlNvlq', 'zhangsan@echo.com', 'USER',  0, '2025-08-22 14:30:00'),
('lisi',     '$2b$10$.MLY9edtiq9lxS1Qm3mvj.M1SzdLOUurwDGSzpCNKKPZlTcvMhVzq', 'lisi@echo.com',     'USER',  0, '2025-09-03 09:15:00');

SET @a = (SELECT id FROM user WHERE username = 'admin');
SET @z = (SELECT id FROM user WHERE username = 'zhangsan');
SET @l = (SELECT id FROM user WHERE username = 'lisi');

-- ═══════════════════════════════════════════════════════
--  帖子（15 篇）：散点分布 2025-09 ~ 2026-03
-- ═══════════════════════════════════════════════════════
INSERT INTO post (user_id, title, content, type, status, comment_count, like_count, score, created_at) VALUES
(@a, '[Java] 并发编程实战：线程池与 CompletableFuture 协同',
 '在日常业务开发中，我们经常遇到需要并发调用多个外部接口的场景。例如在一个电商导购页面中，我们需要同时拉取商品基本信息、库存状态、动态价格、促销优惠券以及用户维度的个性化标签。如果是传统的串行化拉取，假设每个接口平均耗时 200 毫秒，那么五个接口的总耗时就会超过 1 秒钟，这对于首屏渲染来说是完全不可接受的延迟。如果请求量大，还会引起应用服务器线程的严重阻塞。

为了解决这个问题，许多开发者第一反应就是使用 ThreadPoolExecutor 搭配 CountDownLatch 来实现并发查询。然而，原生的 Future 接口依然是阻塞调用的，不仅写法繁琐，而且难以处理任务之间的级联与组合依赖关系。此时，Java 8 引入的 CompletableFuture 就成了解决这类问题的最佳利器。

在实践中，我们首先定义了物理隔离的线程池。对于上述的 IO 密集型接口调用场景，我们不能直接使用系统默认的 ForkJoinPool.commonPool()。因为一旦其中某个第三方服务发生大面积熔断阻塞，默认的公共池就会被迅速耗尽，进而波及应用中其他原本无关的并发业务。所以，我们使用自定义的 ThreadPoolExecutor，同时，我们给队列长度加了限制并使用 CallerRunsPolicy 的拒绝策略，作为反压机制的最后防线。

接着，我们通过 CompletableFuture.supplyAsync() 提交各个拉取任务。对于有依赖逻辑的，比如必须先拉取到商品价格才能计算促销后的最终价，我们通过 thenApplyAsync() 建立这种流水线似的调用关系，代码逻辑瞬间变得如丝般顺滑。

除了并行流转，超时控制也是不可忽视的一环。在真实生产环境里，下游服务偶尔会因为网络抖动出现长尾延迟。如果不设置超时，我们的聚合接口一样会被拖慢。Java 原生提供了 completeOnTimeout()。如果 300 毫秒内由于网络慢没取到活动勋章，我们直接返回默认空值并放行下游。

还有一个必须防范的坑是上下文穿透。我们在开发中发现，MDC 中的 traceId 极其容易在线程切换时丢失，导致错误排查时由于日志链路断裂而完全摸不着头脑。为此，我们封装了一个上下文包装器，在提交任务之前捕获主线程上下文，在新线程真正执行前装载，执行完毕再清理。

采用这套组合拳重构之后，我们商品聚合接口的 TP99 响应时间从原本的 1.1 秒直接优化到了 240 毫秒。实践证明，充分考虑到线程池隔离、超时设计和上下文传递，才能写出高可用的并发程序。', 0, 0, 0, 32, 96, '2025-09-12 09:20:00'),

(@z, '[Spring Boot] 自动配置源码拆解：条件装配的边界',
 '很多同学在使用 Spring Boot 的时候，都觉得它的自动装配机制像是“魔法”。引入一个 starter 依赖，再在 application.yml 里写几行配置，一个复杂的组件就能直接在业务层装配使用。然而，当你的工程越来越大，引入的第三方组件越来越多，甚至你需要为公司内部开发基础平台框架时，如果不理清条件装配的边界问题，很容易踩进依赖冲突和 Bean 覆盖的陷阱。

我在排查一次线上支付模块 Bean 注入冲突异常时，顺着 AutoConfigurationImportSelector 这个核心组件一路向底层阅读源码，整理出了 Spring Boot 自动装配最重要的核心结论。实际上它主要在做三件事：候选配置类的全面收集、基于条件的动态判断，以及最终的延迟导入规则。真正影响系统启动行为和线上稳定性的，往往并不是你写了多少个 @Configuration，而是你对 @Conditional 系列注解使用得是否精准。

对于自动配置类，我强烈建议每一个 @Bean 定义都必须匹配 @ConditionalOnClass 和 @ConditionalOnMissingBean。前者的作用是判断 classpath 下是否有相应的类存在，从而避免 NoClassDefFoundError；后者的作用是当业务工程已经自己通过 @Bean 提供了一个同类型的对象时，我们的自动配置要主动让步，这不仅能避免覆盖业务自定义的 Bean，也是框架设计中“约定大于配置”原则的体现。

在线上调试排障时，我们可以通过在配置里打开 debug 模式来查看 ConditionEvaluationReport，它会把启动时所有的正向命中和反向排除原因清清楚楚地打印出来。如果是单元测试，强烈推荐使用 ApplicationContextRunner 来做一个干净的隔离上下文容器进行验证，它可以模拟缺少依赖类或是指定属性没打开等多种极端场景。

由于某些底层的基础设施如 DataSource、RedisConnectionFactory 等在没有精准条件判断的情况下经常会被意外装配两次，我们团队对所有的内部 starter 进行了一次元代码级改造。我们清晰定义了各个组件生效的前提条件，以及不同环境下的默认缺省值。

这个改造工作完成以后，我们把原本由于大量无效配置加载以及隐式依赖问题拖慢的启动阶段进行了一次彻底的瘦身。不仅解决了业务侧的反馈的灵异覆盖故障，最终通过实测发现整个微服务集群在 K8s 上的平均冷启动时间硬生生缩短了约 18%。搞懂自动装配背后的规则，绝对值得你花两三天的时间！', 0, 0, 0, 27, 85, '2025-09-28 11:45:00'),

(@l, '[Redis] 缓存策略组合拳：穿透、击穿、雪崩全链路治理',
 '在后端高并发场景中，Redis 往往是抵挡海量流量的首要防线。相比于查询速度可能不够快，缓存在生产环境中最怕的其实是处于失控状态。我在重构公司核心的内容分发和个性化推荐服务时，由于流量的暴增，接连遇到了缓存的透穿、击穿以及瞬间大面积失效带来的雪崩效应。借此机会，我主导对整个服务的缓存链路做了一次彻头彻尾的治理优化。

第一层我们要防范的是明显的恶意攻击和不规范请求带来的缓存穿透。因为如果有人频繁用根本不存在的主键去请求接口，不仅缓存层面无法拦下，而且这些压力会全部如实地向后打向数据库。为此我们在入口网关处引入了基于 Redis BitMap 实现的布隆过滤器拦截方案。对于不存在的资源，直接在这一层给出空响应；即使有极低的哈希冲突误判，我们也在业务逻辑里给数据库中的空值短暂缓存了 60 秒的 null，保证拦截率的高效防线。

第二层是针对突发热点数据的缓存击穿问题。例如某位明星发了一条热点引流的视频导致大量请求在同一瞬间涌入，而刚好这个资源的缓存在前一秒过期了。如果几万个请求并行去数据库做回源操作并构建复杂对象，数据库必定当场瘫痪。为此我们在回源重建的逻辑链条上加入了轻量级的分布式层面的互斥锁机制 (基于 SET NX 实战)，以此确保同一时刻仅仅放行一个幸运请求去重建缓存，其余请求进行短暂 sleep 然后重新查缓存获取结果。

第三层是对批量产生的短时效性 Key 进行的雪崩治理。最典型的就是我们在每天午夜大跑批更新数万个资源并设定了完全一致的过期时间，这无疑埋下了一颗将在第二天中午同时引爆的定时炸弹。我们在原先的固定基础缓存有效时间上附加了一个 1~5 分钟的随机漂移范围因子，从根源上把集中掉落的流量给平滑地推移稀释掉。

对于系统里极少数对强一致场景要求苛刻的状态位字段，我们不再走简单的延时双删，而是改成了先更新数据库后删除缓存，并通过基于 RocketMQ 消息队列监听变更日志的方式来做一个极高可靠性的二次删除兜底。伪代码结构为：updateDb() -> deleteCache() -> sendDelayDeleteEvent()。

此外，我们在系统运营层面做出了一个关键改变。很多时候大家都只会抱怨 Redis 的缓慢，实际上很多所谓的内存问题或命令阻塞，其源头往往是来自于下游 SQL 查询能力的灾难级退化以及未命中的回源锁死。所以我们将所有慢查询接口的监控指标和业务的缓存击穿命中率放进了同一张看板。

这一系列链路治理改造后，在双 11 以及日夜秒杀等各种流量狂欢时期，我们整个业务集群的高峰期缓存命中率稳如泰山地保持在 96% 的安全线之上，核心数据库的连接数由于彻底屏蔽了爆发型的脉冲流量，再也没有出现过满负荷抖动的情况，系统的整体可用性跃升至一个新的历史台阶。', 0, 0, 0, 41, 128, '2025-10-15 14:30:00'),

(@a, '[MySQL] 索引优化路线图：从慢 SQL 到稳定低延迟',
 '作为后端开发，面对线上偶尔突发的数据库慢查询，很多人的第一反应就是在 WHERE 条件相关的字段上建个单列索引。这种“头痛医头”的做法，初期可能有效，但随着表数据量突破千万规模、业务查询维度越来越复杂，胡乱建索引不仅会导致插入和更新性能雪崩，还可能因为走错了索引而引发 Filesort 内存溢出或是庞大的物理全表回表扫描。针对这个问题，我们的技术团队内部最终总结并强制推行了一套标准化的索引优化打法，从而彻底稳住了核心大促节点的延迟指标。

第一步也是最核心的抓取源头：开启慢日志（Slow Query Log）监控并且务必结合业务维度的 APM 和全链路追踪 traceId。只有明确那条拖累整个系统 QPS 最严重的慢 SQL 在真实的接口表现，我们才能去精准进行开刀。不要被各种偶尔出现的夜间大数据批处理报表提取所迷惑，重点必须且绝对要锁定在白天高频的核心 C 端在线交易流里出现的那些反复超时操作上。

第二步：使用 EXPLAIN 与 EXPLAIN ANALYZE 剖析探针。这是最能体现 DBA 与普通开发差距的基础命令所在。我们要关注的核心指标除了 type（必须努力确保在 range 以上甚至 ref、eq_ref，绝对避免出现 ALL），更要高度注意 Extra 提示列。如果你的查询里看到了 Using filesort 或是非常影响性能的 Using temporary，那就意味着当前的联合索引并未完全覆盖到业务涉及的 ORDER BY 和 GROUP BY 语句，内存排序已经悄然成为了制约当前请求效率的关键地雷。

第三步，严格遵循最左前缀复用规则去合并构建高质量联合索引，而非继续累加单列盲目索引。在设计组合时，我们应该永远优先考虑将区分度（基数 cardinality）最高的纯前缀匹配列放在最前，随后才是紧跟着那些经常涉及到范围判断和用来进行业务特定排序属性的对象。此外，巧妙的加入覆盖索引（Covering Index）思路，也就是把那些极其常用但不需要进行重度排序以及高计算消耗返回数据的极少数业务结果列数据字段顺手也放在索引构建包含最尾端部位，可以产生彻底并完全消除需要额外根据主键树再一次产生重度磁盘随机读取的惊人开销收益。

第四步：强制梳理并制定下线冗余垃圾历史索引规则。任何在一张高频发生写入和擦除变化的订单业务大表表面绑定附加上过多无用的 B+ Tree 结构，都等于在给数据库的主干核心处理心跳带上沉重无比的石头。对于由于产品下线需求或是版本更替而再也没有高频流量经过打到的各种旧有字段历史冗余遗留废弃垃圾索引，要有计划且无情地清理拔除。

遵守这样的演进路线对一张包含了近八千万级别的核心履约单据流水库主表整改后，我们在没有额外升级高阶机器资源的情况下，单接口压测 TP99 响应延迟居然硬生生地从危险的 800+ 毫秒断崖式缩减至不可思议的 15 毫秒以下，数据库的连接阻塞情况彻底被消磨于无形。', 0, 0, 0, 52, 118.5, '2025-09-15 10:20:00'),

(@z, '[Docker] 容器化交付规范：多阶段构建与日志标准化',
 '团队之前的 Dockerfile 写法非常粗糙：一个 FROM openjdk:17 拉下来就往里面 COPY 源码、mvn package、然后 CMD 启动。结果镜像体积 1.2 GB，CI 流水线光推镜像就要 8 分钟，而且镜像里残留了完整的源代码和 Maven 工具链，存在安全隐患。

我们推行的第一项改造是多阶段构建（Multi-Stage Build）。Dockerfile 拆成两个 FROM 阶段：第一阶段 builder 用 maven:3.9-eclipse-temurin-17 做编译打包，输出一个 jar 文件；第二阶段用 eclipse-temurin:17-jre-alpine 只拷贝 jar 进去。改造后镜像从 1.2 GB 瘦身到 180 MB，CI 推送时间降到 40 秒。Alpine 基础镜像还自带最小化攻击面。

第二项改造是日志标准化。之前各服务把日志写到容器内 /app/logs/ 目录，运维要 docker exec 进去才能看，而且容器重启日志就丢了。我们强制所有应用日志输出到 stdout/stderr，通过 Logback 配置 ConsoleAppender + JSON 格式化（LogstashEncoder）。每行日志是一个完整的 JSON 对象，包含 timestamp、level、traceId、service、message 字段。Filebeat 以 DaemonSet 方式采集容器标准输出，直传 Elasticsearch。

这样任何服务故障都能在 Kibana 上通过 traceId 一键关联全链路日志，排障效率提升了一个数量级。', 0, 0, 0, 45, 98, '2025-10-02 14:15:00'),

(@l, '[微服务] 服务拆分方法论：按业务能力而不是按数据库',
 '接手过一个从单体硬拆出来的微服务项目，结果比单体更难维护。原因很典型：当初拆分的依据是数据库表——用户表一个服务、商品表一个服务、订单表一个服务。看似清晰，实际上一次下单操作要跨三个服务做 RPC 调用和分布式事务，延迟比单体时代翻了 5 倍，而且任何一个服务挂掉整条链路全挂。

正确的拆分应该以业务能力（Business Capability）为边界，而不是数据库表。我们用 DDD 的限界上下文重新划边界：把商品信息、库存、价格计算收进「商品域」服务；把下单、支付、履约状态收进「交易域」服务。每个域内部拥有自己完整的数据存储，域之间只通过领域事件异步通信。重构后单次下单从 4 次跨服务同步调用缩减为域内本地事务 + 1 条异步消息。

具体落地时有几个关键判断标准：如果两个功能总是一起变更发版，它们应该在同一个服务；如果两个功能可以独立上线互不影响，它们适合拆开。我们还引入了 Context Map 画出各域之间的上下游关系，明确哪些是 Conformist、哪些是 Anti-Corruption Layer。按这套方法论重新拆分完成后，服务间耦合度下降明显，新功能迭代周期从两周缩短到三天。', 0, 0, 0, 76, 135, '2025-11-05 16:30:00'),

(@a, '[Java] JVM 内存与 GC 调优：线上故障复盘与修复',
 '去年双 11 大促前压测，订单服务出现周期性卡顿，TP99 每隔 15 分钟飙到 3 秒。通过 GC 日志分析，发现是 Full GC 频繁触发——每次暂停 1.2 秒。根因是大量订单聚合对象在 Young GC 时存活率过高，Survivor 区装不下直接晋升老年代，老年代很快被填满。

第一步调整 JVM 参数：将 -Xmn 从 512m 提升到 1.5g（总堆 4g），-XX:SurvivorRatio 从 8 改为 6，给 Survivor 更多空间。同时从 Parallel GC 切换到 G1，设置 -XX:MaxGCPauseMillis=200。第二步排查代码：发现一个批量查询接口一次加载 2000 条订单明细到 List，改为分页流式处理后单次内存占用降了 80%。

第三步加入 OOM 防护线：配置 -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/data/dumps/，并用 MAT 分析了一次历史 dump，揪出了一个 HashMap 缓存未设上限导致的慢性泄漏。修复后压测 Full GC 完全消失，Young GC 频率从每秒 2 次降至每 5 秒 1 次，TP99 稳定在 50ms 以下。', 0, 0, 0, 62, 105, '2025-12-10 11:45:00'),

(@z, '[Spring Boot] 事务失效的 8 个高频坑位清单',
 '整理了我们团队踩过的 Spring 事务失效问题，每一个都曾造成线上脏数据。第一个坑：同类方法内部自调用。由于 @Transactional 依赖 AOP 代理，this.methodB() 不会经过代理，事务注解直接失效。解法是注入自身代理或使用 AopContext.currentProxy()。

第二个：异常被 try-catch 吞掉。Spring 默认只对 RuntimeException 回滚，如果 catch 块没有重新抛出异常，事务感知不到错误就会提交脏数据。第三个：方法修饰符不是 public。Spring AOP 对 protected 和 private 方法不生效。第四个：propagation 配置错误，REQUIRES_NEW 与 REQUIRED 混用导致内层事务回滚时外层不感知。

第五个：rollbackFor 未指定 Exception.class，导致 Checked Exception 不回滚。第六个：多数据源场景下 DataSourceTransactionManager 绑定错了 DataSource。第七个：@Async 和 @Transactional 同时使用，异步线程脱离了原始事务上下文。第八个：在 synchronized 块内标注事务，锁在事务提交前释放，其他线程读到中间状态。

我们的治理方式是在 CI 流水线加入 ArchUnit 规则扫描，强制检查 @Transactional 的使用规范，上线后事务相关故障降为零。', 0, 0, 0, 88, 142, '2026-01-25 09:30:00'),

(@l, '[前端] Vue3 + TypeScript 大型项目架构实践',
 '当我们把团队里的十几个中后台项目全面从 Vue2 迁移到 Vue3 + TypeScript 后，工程化的红利终于得到了彻底的释放。最大的改变首先来源于 Composition API（组合式 API）对重度复杂业务逻辑的彻底拆解。以前在 Options API 时代，一个包含表单校验、下拉级联、长列表查询分页并且附带有各种状态追踪的超级大组件，其内部的 data、computed、methods 往往横跨几千行，维护起来犹如灾难。在 Vue3 下，我们将这些特定业务领域的逻辑块完美抽离成了各种相互独立的高内聚 useHooks，比如 useTablePagination、useFormValidator 等。这不仅在多个页面之间实现了惊人的代码复用率，更是让单组件的代码体积暴瘦 60% 以上。

其次，TypeScript 的全方位深度介入也是本次重构最为亮眼的成果之一。不仅是对后端接口返回的复杂 JSON 数据建立了极其严密的 Interface 定义防止空指针或类型错位，我们更是在组件间通讯（Props 和 Emits）上建立了死板但极度安全的类型约束。借助于 defineProps 和泛型的结合，任何非法的传参都会在 VSCode 编码阶段瞬间飘红阻断，将大量的由于格式不匹配导致的线上 Runtime 错误拦截在了开发环境。结合 Pinia 这种本身就对 TS 极其友好的状态管家，前端数据流流转终于变得清晰且透明。

在目录规范上，我们也摒弃了早期那种仅仅按视图 (views) 和组件 (components) 简单划分的初级模式。基于业务功能模块化理念，我们将路由、状态、API 请求封装、类型声明以及相关组件统统收纳到了以具体业务实体为名的功能包结构下。通过这些架构重组，现在新同学加入项目只需半天即可流利地上手跟进迭代，整体前端基建水平跃升了一个大台阶。', 0, 0, 0, 32, 86.5, '2026-02-05 10:20:00'),

(@a, '[Go] 协程调度与 Channel：高并发任务系统设计笔记',
 '最近用 Go 重写了一个日志清洗服务，原来的 Java 版本用 ThreadPoolExecutor + BlockingQueue 实现，256 线程扛 2 万 QPS 时 CPU 已经 90%，主要开销在线程上下文切换。Go 版本用 1000 个 Goroutine 轻松扛到 8 万 QPS，CPU 才 45%。Goroutine 的栈初始只有 2KB（线程是 1MB），上下文切换在用户态完成，代价比系统级线程低两个数量级。

架构采用经典的 Worker Pool 模式：main 函数启动一个 Producer goroutine 从 Kafka 消费消息，解析后丢进 jobs channel；另外启动 N 个 Worker goroutine 从 jobs channel 取任务做清洗和写入。Channel 的缓冲大小是关键调优参数：设太小生产者会被阻塞，设太大内存会暴涨。我们通过压测确定了 buffer = Worker 数量 x 2 的经验值。

错误处理方面，每个 Worker 内部用 recover() 兜住 panic 防止单条消息异常导致整个 Worker 退出。更重要的是使用 errgroup 管理 Worker 生命周期：任何一个 Worker 返回致命错误时，errgroup 自动调用 context cancel 通知所有 Worker 优雅退出。这样既防止了 Goroutine 泄漏，又保证了服务级别的错误传播。整套方案跑了半年没出过一次资源泄漏问题。', 0, 0, 0, 55, 110, '2026-02-18 16:45:00'),

(@z, '[DDD] 领域驱动重构实录：从贫血模型到聚合根',
 '接手一个三年历史的电商订单系统时，代码结构典型的贫血模型：Order 实体只有字段和 getter/setter，所有业务逻辑散落在 OrderService 的 2800 行代码里。改一个状态流转规则要翻遍整个 Service，而且经常漏改导致状态不一致。

重构第一步是识别聚合根。我们把 Order 定义为聚合根，OrderItem、PaymentRecord 作为其内部实体。核心规则：外部只能通过 Order 的业务方法操作内部状态，不允许直接修改属性。比如之前代码里到处写 order.setStatus(PAID)，重构后必须调用 order.pay(paymentInfo)，方法内部校验当前状态是否允许支付、计算实付金额、记录支付流水，一气呵成。

第二步是约束状态机。我们在 Order 内部维护了一个状态转移表：CREATED 只能到 PAID 或 CANCELLED，PAID 只能到 SHIPPED 或 REFUNDING。任何不合法的状态转移在 Order 方法里直接抛 IllegalStateException。这比之前 Service 层散落的 if-else 判断可靠得多。

重构后 OrderService 从 2800 行瘦身到 400 行，只剩编排逻辑和基础设施调用。单元测试覆盖率从 15% 提升到 82%——因为核心规则都在纯 Java 对象里，不需要 Mock 数据库和 Spring 容器就能测试。最直观的收益是新需求开发效率翻倍，状态相关的线上 Bug 归零。', 0, 0, 0, 48, 105, '2026-02-12 09:30:00'),

(@l, '[Redis] 分布式锁正确姿势：Redisson 实战与坑点',
 '秒杀场景下库存扣减必须做并发控制，最初我们用 SETNX + EXPIRE 实现分布式锁，结果上线就出了超卖事故。原因是这两条命令不是原子操作：SETNX 成功后如果服务宕机，EXPIRE 没来得及执行，锁就永远不会释放——死锁。即使用 SET key value EX 30 NX 解决了原子性，仍然有更隐蔽的问题：业务执行超过 30 秒锁自动过期，第二个请求进来拿到锁，第一个请求结束时 DEL 掉的是第二个请求的锁。

切换到 Redisson 后这些问题迎刃而解。Redisson 的 RLock 底层通过 Lua 脚本保证加锁原子性，锁的 value 包含线程标识（UUID + threadId），释放时只能删自己的锁。关键特性是 WatchDog 机制：加锁成功后后台线程每 10 秒（leaseTime/3）自动续期，业务没执行完锁就不会过期；如果服务宕机 WatchDog 停止续期，锁在 30 秒后自然释放，不会死锁。

但 Redisson 在 Redis 主从架构下有一个已知缺陷：客户端在 Master 上加锁成功，数据还没同步到 Slave 时 Master 宕机，Sentinel 将 Slave 提升为新 Master，此时另一个客户端可以再次加锁成功——出现双写。对于金融级场景，我们的方案是在加锁后追加一次数据库乐观锁校验（版本号），双重保险确保不超卖。RedLock 算法虽然理论上解决了这个问题，但实现复杂且存在时钟漂移争议，生产中慎用。', 0, 0, 0, 72, 125, '2026-02-10 07:40:00'),

(@a, '[MySQL] 读写分离后的数据一致性：延迟感知与补偿',
 '当业务量级突破单库性能瓶颈后，MySQL 读写分离几乎是必经之路。我们将写操作路由到主库、读操作分流到多个从库，QPS 承载能力提升了近 4 倍。但上线第一周就收到大量用户投诉：刚发布的内容刷新后看不到，订单状态更新了页面却不变。核心原因是主从复制延迟——在高峰期从库滞后主库 200~800 毫秒。

我们的治理方案分三层。第一层是写后读强制走主（Read-after-Write Consistency），在 Service 层通过 ThreadLocal 标记当前请求是否执行过写操作，拦截器据此将后续读请求强制路由到主库，标记有效期设为 2 秒。第二层是关键业务接口直接标注 @Master 注解绕过从库，比如支付回调、库存扣减确认。第三层是引入半同步复制（Semi-Sync Replication），至少一个从库确认收到 Binlog 后主库才提交，将数据丢失窗口缩小到接近零。

此外我们在中间件层加了延迟监控探针，每 5 秒采集 Seconds_Behind_Master 指标推送到 Prometheus。当延迟超过 1 秒时自动触发告警，超过 3 秒时降级为全量走主库。上线后用户侧一致性投诉完全消失，同时从库利用率维持在 65% 左右，达到了性能与一致性的平衡。', 0, 0, 0, 41, 93, '2026-02-21 21:10:00'),

(@l, '[系统架构] 可观测性建设：日志、指标、链路三位一体',
 '微服务拆分后最大的运维痛点是故障定位：一个请求跨 6 个服务，哪一环出了问题？我们用了半年时间搭建了基于日志、指标、链路追踪的三位一体可观测性平台。

日志层面，所有服务统一使用 Logback + LogstashEncoder 输出结构化 JSON 日志到 stdout，核心字段包括 timestamp、traceId、spanId、service、level、message。通过 Filebeat 采集后灌入 Elasticsearch，在 Kibana 上建了按 traceId 聚合的日志检索视图，一条 traceId 即可串联全链路所有日志。

指标层面，使用 Micrometer + Prometheus 暴露 JVM 内存、GC 次数、接口 QPS、响应耗时 P50/P99 等关键指标。Grafana 上为每个服务建了标准化 Dashboard 模板，包含 RED 三指标（Rate、Errors、Duration）。配合 AlertManager 设置了分级告警：P99 超 500ms 黄色预警，错误率超 1% 红色告警直接打电话。

链路追踪层面，我们从 Zipkin 迁移到 OpenTelemetry Collector + Jaeger 后端。OTel SDK 在 Spring Boot 中通过 Java Agent 自动埋点，零侵入采集 HTTP 调用、数据库查询、Redis 操作的 Span 数据。上线后排障时间从平均 45 分钟缩短到 5 分钟以内，三个维度交叉验证让任何性能劣化都无处遁形。', 0, 0, 0, 68, 115, '2026-03-12 15:00:00'),

(@z, '[Vue.js] 前端性能优化：首屏、缓存与构建链路协同',
 '在大前端时代，C 端项目首屏哪怕只慢了 0.5 秒，用户的跳出率都会呈现指数级的爆发增长。最近我们在优化公司核心商城 H5 时经历了一次长达两周的疯狂攻坚战。之前项目的首屏加载需要足足 3.2 秒，各种 vendor 依赖包加起来有近乎疯狂的 2MB 大小，在弱网环境下简直是灾难。

第一步开场就是最为暴力的依赖体检瘦身。我们通过 webpack-bundle-analyzer 揪出了好几个因为引用不规范导致被全量打包进来的诸如 lodash 和 moment.js 等超级巨兽。改用基于 ES Module 的按需导入，并且用体积犹如鸿毛的 dayjs 替换掉历史沉疴。第二步则是切断了原本粗暴的整块应用单包输出模式，把路由彻底落实为了基于魔法注释配置的代码拆包动态 Lazy 加载模式。仅仅这最基础的两板斧，包体积就暴跌了 65%。

然而光缩小体积远远不够，如果不搭配高效的利用各种外部网络请求策略仍然无法达到最佳体验。我们引入了极其有策略预判意味的 Preload 和 Prefetch 特性标记资源，让浏览器在主干业务空闲时去偷偷提前把下游大概率会访问的依赖搬回来。再搭配上把一切几乎都不会轻易改动的静态资源（图片、字体乃至底层的 Vue 运行时框架代码本身）强力推向云端进行多地分发全网极其暴力的强缓存 CDN 策略，最终大提速。最终压测首屏竟然奇迹般地压至 0.86 秒！性能优化绝对不仅仅是几个花哨的 API 技巧，而是涵盖了构建、网络、渲染整条大链路的工程化战役。', 0, 0, 0, 45, 96, '2026-03-03 10:25:00');

-- ═══════════════════════════════════════════════════════
--  专栏文章（9 篇）：散点分布 2025-10 ~ 2026-03
-- ═══════════════════════════════════════════════════════
INSERT INTO column_article (user_id, column_name, title, content, cover_img, status, like_count, view_count, created_at) VALUES
(@a, 'Java 核心技术', '深入理解 Java 内存模型与可见性边界',
 'JMM（Java Memory Model）定义了线程之间共享变量的可见性规则。核心问题是：一个线程修改了共享变量，另一个线程什么时候能看到？答案取决于 happens-before 关系是否成立。最常见的踩坑场景是用一个普通 boolean 标志位做线程间通信：主线程设 running = false，Worker 线程的 while(running) 循环却不退出，因为 Worker 读的是 CPU 缓存中的旧值。

解法很直接：给 running 加 volatile 关键字。volatile 保证写操作立即刷到主存、读操作从主存取值，建立 happens-before 语义。但 volatile 只保证可见性，不保证原子性——i++ 这种复合操作仍然需要 AtomicInteger 或同步块。

另一个隐蔽问题是指令重排序。经典的双重检查锁单例如果不给 instance 加 volatile，构造函数的初始化可能被重排到引用赋值之后：线程 B 拿到非 null 引用但对象还没初始化完毕。JDK 5 之后 volatile 禁止了这种重排序。

实战建议：共享标志位用 volatile；复合读写用 Atomic 类；需要多步原子操作用 synchronized 或 ReentrantLock。选型时画清每个共享变量的 happens-before 链，比背 JMM 规范有效得多。', 'https://img.echo.dev/column/java-jmm.png', 0, 46, 860, '2025-10-08 09:00:00'),

(@a, 'Java 核心技术', '手写线程池前必须掌握的四个设计点',
 '手写过一个简化版线程池后，才真正理解 ThreadPoolExecutor 的设计精妙。四个核心设计点缺一不可。

第一是工作队列的选择。无界队列（LinkedBlockingQueue）虽然不会拒绝任务，但会在突发流量下积压到 OOM。有界队列（ArrayBlockingQueue）配合合理的拒绝策略才是生产级配置。我们用 capacity=500 + CallerRunsPolicy 做反压：队列满了让提交线程自己执行，自然降速。

第二是线程数计算。CPU 密集型任务设为 CPU 核心数+1；IO 密集型任务按公式 N = CPU核心 x (1 + IO等待时间/CPU计算时间) 估算。我们的订单导出服务大量调数据库属于 IO 密集型，8 核机器设 24 线程，吞吐量比默认 8 线程提升了 2.5 倍。

第三是线程工厂命名。默认的 pool-1-thread-3 在日志排查时毫无辨识度。自定义 ThreadFactory 给线程命名为 order-export-1 之后，jstack 一眼就能定位问题。

第四是优雅关闭。shutdown() 等待已提交任务完成但不接受新任务；shutdownNow() 尝试中断所有线程。生产中用 shutdown() + awaitTermination(30, SECONDS) + shutdownNow() 三段式关闭，确保不丢任务又不卡进程。', 'https://img.echo.dev/column/java-threadpool.png', 0, 39, 730, '2025-10-25 11:10:00'),

(@z, 'Java 核心技术', 'Spring 事务传播行为在真实项目中的选型',
 'Spring 定义了 7 种事务传播行为，但实际项目 90% 场景只用到 3 种。REQUIRED 是默认值：有事务就加入，没有就新建。适合绝大多数 Service 方法。REQUIRES_NEW 强制新建独立事务：外层回滚不影响内层。我们在审计日志记录时使用——即使业务操作失败回滚，审计记录必须保留。NOT_SUPPORTED 以非事务方式执行：我们在发送通知邮件时使用，避免邮件发送耗时拉长事务持有连接的时间。

最常犯的错误是在 REQUIRED 方法内调用 REQUIRES_NEW 方法时，没意识到内层方法如果抛异常，外层 catch 后继续执行会导致事务状态被标记为 rollback-only，最终外层提交时抛 UnexpectedRollbackException。解法是确保外层要么不 catch 要么重新 throw。

另一个坑是嵌套事务（NESTED）与 REQUIRES_NEW 的混淆。NESTED 用 SavePoint 实现，是真正的子事务——父事务回滚子事务也回滚，但子事务回滚不影响父事务。REQUIRES_NEW 则是两个完全独立的物理事务。实际选型时：需要局部回滚用 NESTED，需要完全隔离用 REQUIRES_NEW。建议团队内部统一使用规范，并在 Code Review 时重点审查嵌套事务场景。', 'https://img.echo.dev/column/java-tx.png', 0, 33, 642, '2025-11-12 16:40:00'),

(@l, 'Java 核心技术', 'JDK 17 在后端项目中的落地收益与注意项',
 '团队从 JDK 8 升级到 JDK 17 经历了三个月的渐进式迁移，收益远超预期。最直观是 Records 类型：之前 DTO 类动辄 80 行（字段 + getter + setter + equals + hashCode + toString），换成 record UserDTO(Long id, String name, String email) {} 一行搞定，代码量砍了 70%。

Sealed Classes 在我们的状态机设计中特别好用。声明 sealed interface OrderEvent permits CreateEvent, PayEvent, CancelEvent，编译器会强制 switch 覆盖所有子类型，漏写直接报错。比之前用枚举 + instanceof 判断安全得多。

Text Blocks 终于让 SQL 模板可读了：之前多行 SQL 拼接用一堆 + 号和转义引号，现在三引号块直接写，格式清晰还支持缩进对齐。

性能层面，G1 GC 在 JDK 17 的优化相当明显：相同负载下 Young GC 暂停时间缩短了约 20%。ZGC 也已经生产就绪，我们的实时报价服务切到 ZGC 后暂停时间从 50ms 降到 sub-1ms。

迁移注意事项：javax 包改 jakarta 影响最大（如果用 Spring Boot 3）；反射访问内部 API 需要加 --add-opens 参数；部分三方库对 JDK 17 模块系统不兼容需要升级版本。建议先在 CI 上跑全量测试再分批上线。', 'https://img.echo.dev/column/java-jdk17.png', 0, 28, 580, '2025-12-01 13:20:00'),

(@z, '前端工程化实践', 'Vite 构建加速实战：缓存命中率是核心指标',
 '项目从 Webpack 迁到 Vite 后，开发服务器启动从 28 秒降到 300 毫秒——因为 Vite 在 dev 模式用原生 ESM 按需编译，不做全量打包。但生产构建（vite build 底层用 Rollup）初期反而比 Webpack 慢了 15%，优化后才真正释放性能。

关键一：依赖预构建缓存。Vite 首次启动会用 esbuild 把 node_modules 中的 CommonJS 依赖（如 lodash、axios）转成 ESM 并缓存到 node_modules/.vite。只要 package.json 不变，后续启动直接用缓存。但我们的 CI 每次 clean install 会清掉缓存——在 CI 脚本里加 cache node_modules/.vite 后构建时间减少 40%。

关键二：手动 chunks 分割。用 manualChunks 把 vue + vue-router + pinia 拆为 vendor-vue、element-plus 拆为 vendor-ui、业务代码按路由拆。用户更新业务代码时 vendor 包的 hash 不变，浏览器强缓存命中。我们监控显示二次访问的网络请求量下降 75%。

关键三：开启 CSS Code Split。Vite 默认把每个异步 chunk 关联的 CSS 单独提取，避免一个巨大的 style.css 阻塞首屏。配合 Gzip/Brotli 压缩插件，最终首屏加载体积从 1.8MB 压到 420KB。', 'https://img.echo.dev/column/fe-vite.png', 0, 37, 701, '2025-12-20 10:30:00'),

(@l, '前端工程化实践', 'Vue3 组件库设计：从 API 稳定性到类型安全',
 '为公司内部 10+ 个中后台项目构建了统一组件库，踩坑最深的不是组件开发本身，而是 API 设计的稳定性。发布 1.0 后一次不慎的 breaking change（把 visible prop 改名为 open）导致 3 个项目同时构建失败，教训惨痛。

从此我们确立了组件 API 三原则。一是 Props 命名参照 Element Plus / Ant Design 惯例，降低使用者迁移认知成本。二是用 defineProps 的泛型写法严格定义类型，任何新增 prop 必须给默认值保证向后兼容。三是废弃 prop 不直接删除，先标记 @deprecated 并在 console.warn 提示，至少保留两个大版本的过渡期。

类型安全方面，我们给每个组件导出 Props 和 Emits 类型定义。使用侧 import type { ButtonProps } from "@echo/ui" 后可以对 props 对象做完整类型检查。配合 Volar 的模板类型推导，传错 prop 类型在 IDE 里实时飘红。

构建侧用 Vite Library Mode 输出 ESM + CJS 双格式，配合 package.json 的 exports 字段做条件导出。样式用 CSS Variables 定义主题 token，使用方只需覆盖变量就能换肤，不需要引入 Sass。每次发版自动生成 Changelog 和组件文档站。', 'https://img.echo.dev/column/fe-component.png', 0, 31, 612, '2026-01-10 15:45:00'),

(@a, '前端工程化实践', '前端监控平台从 0 到 1：错误、性能与用户行为统一分析',
 '线上页面白屏了用户截图发群里，开发才知道出了 Bug——这是我们搭建前端监控平台的直接动机。从零开始做了三件事：错误监控、性能采集、用户行为追踪。

错误监控：用 window.onerror 捕获运行时异常，window.addEventListener("unhandledrejection") 捕获 Promise 错误，Vue.config.errorHandler 捕获组件渲染异常。每条错误上报时带上 stack trace、当前路由、用户 ID 和浏览器信息。后端按 stack fingerprint 聚合去重，避免同一个 Bug 产生几万条重复告警。上线第一周就发现了 3 个隐藏了两个月的低频 Bug。

性能采集：通过 PerformanceObserver API 收集 LCP、FID、CLS 三个 Core Web Vitals 指标，加上首屏白屏时间（FP/FCP）和接口耗时。数据按页面路由维度聚合，Grafana 上能看到每个页面的性能趋势。设置红线：LCP 超过 2.5 秒的页面自动创建优化 Issue。

用户行为追踪：在关键交互节点（按钮点击、页面跳转、表单提交）埋点，生成行为序列。当某个用户反馈 Bug 时，通过 sessionId 回溯他的完整操作路径，10 分钟定位复现步骤。数据存储用 ClickHouse 做列式分析，日均千万级事件查询秒回。', 'https://img.echo.dev/column/fe-monitor.png', 0, 35, 655, '2026-01-28 09:50:00'),

(@a, '分布式系统设计', '幂等、重试与补偿机制的统一设计模型',
 '分布式系统中网络不可靠是常态，任何远程调用都可能因为超时重试而重复执行。如果接口不幂等，重试一次可能扣两次款。我们团队建立了一套统一的幂等设计标准。

核心方案是请求唯一标识（Idempotency Key）。客户端在请求头带上全局唯一的 requestId，服务端先查 Redis 是否已处理过这个 ID。如果存在，直接返回上次的结果；不存在则执行业务逻辑，完成后将结果缓存到 Redis 并设 24 小时过期。Lua 脚本保证 check-and-set 的原子性。

对于数据库写操作，我们用唯一索引做兜底：订单号 + 操作类型建联合唯一索引，重复插入报 DuplicateKeyException 直接返回成功。比分布式锁轻量得多。

重试策略采用指数退避：第一次立即重试，第二次等 1 秒，第三次等 2 秒，最多 3 次。配合 Circuit Breaker（Resilience4j），连续失败 5 次自动熔断 30 秒，防止重试风暴压垮下游。

对于无法重试的长事务（如跨服务转账），用 Saga 补偿模式：每个步骤定义正向操作和补偿操作，任何步骤失败后依次执行已完成步骤的补偿方法。通过事件表 + 定时扫描保证补偿一定会执行到。', 'https://img.echo.dev/column/dis-idempotent.png', 0, 42, 790, '2026-02-15 12:00:00'),

(@z, '分布式系统设计', '高可用架构中的限流与降级：从策略到落地',
 '去年 618 大促期间，一个上游营销系统突然推送了 10 倍于预期的流量，导致我们的商品详情服务 CPU 打满、响应超时，连带影响了首页和搜索。根因是没有做入口限流。

事后我们基于 Sentinel 搭建了三层防护。第一层是网关层 QPS 限流：根据服务容量压测结果，给每个 API 设置 QPS 上限。超过阈值的请求直接返回 429 状态码和友好提示。预热模式（Warm Up）让系统启动后流量缓慢爬升，避免冷启动被打崩。

第二层是服务间熔断降级。商品详情依赖价格服务、库存服务、评价服务。通过 Sentinel 对每个下游调用设置慢调用比例熔断规则：如果 10 秒内超过 50% 的请求耗时超过 500ms，自动熔断 30 秒。熔断期间走 fallback 逻辑——价格显示"获取中"、评价显示"暂无"，核心的商品信息和库存始终可用。

第三层是针对突发热点的自动探测。利用 Sentinel 的热点参数限流，对高频访问的 skuId 自动识别并单独限流。某个爆款商品被疯狂刷新时只限制它，不影响其他商品的正常访问。

效果立竿见影：618 期间流量峰值达到日常的 12 倍，核心接口可用率保持在 99.95% 以上，零故障大促。', 'https://img.echo.dev/column/dis-degrade.png', 0, 44, 830, '2026-03-05 18:20:00');

-- ═══════════════════════════════════════════════════════
--  面经（9 篇）：散点分布 2025-10 ~ 2026-03
-- ═══════════════════════════════════════════════════════
INSERT INTO interview (user_id, company, position, difficulty, result, title, content, like_count, view_count, created_at) VALUES
(@z, '阿里巴巴', 'Java 后端', 4, 'offer', '阿里巴巴 Java 后端三面复盘：高并发与设计能力并重',
 '一面 45 分钟纯基础：HashMap 的 put 流程和扩容机制、ConcurrentHashMap 在 JDK 8 的分段锁改进、ThreadLocal 的内存泄漏原因（Entry 弱引用 key 但强引用 value）。答得比较流畅，面试官追问了如果 HashMap 的 key 是自定义对象需要注意什么——必须重写 hashCode 和 equals 且保证不变性。

二面围绕项目深挖。我讲了订单系统的库存扣减方案，面试官连续追问：Redis 预扣和数据库扣减不一致怎么办？我回答了最终一致方案（消息队列 + 对账补偿）。又问如果 Redis 宕机了怎么降级？我描述了 Sentinel 切换期间暂停接单 + 本地缓存兜底的方案。这一轮的体会是：面试官不在乎你用了什么组件，而是看你能不能在故障场景下做出合理的取舍判断。

三面是系统设计：设计一个支持百万用户同时参与的秒杀系统。我从前端静态化 CDN、网关层限流、Redis 原子扣减、异步下单消息队列、数据库最终落盘的五层架构展开，重点讲了热 key 打散和库存预分片的细节。总时长 40 分钟，最后 HR 面聊了职业规划。三天后收到 offer。', 34, 920, '2025-10-05 20:10:00'),

(@l, '腾讯', '前端', 3, 'rejected', '腾讯前端面试记录：基础扎实但工程化表达不足',
 '一面考察前端基础。先问了事件循环机制：宏任务和微任务的执行顺序，给了一段包含 setTimeout、Promise.then、async/await 的代码让判断输出顺序。接着问 Vue3 响应式原理：Proxy 替代 Object.defineProperty 的优势（能监听数组下标和新增属性），reactive 和 ref 在源码层面的区别。这部分答得不错。

二面上来直接写代码。第一题是实现一个带并发控制的请求调度器 Scheduler(maxCount)，要求 add 方法返回 Promise。我用了一个队列 + 计数器的方案，面试官补充问了如果需要支持优先级怎么改（换成优先队列）。第二题是手写 Promise.all，边界情况包括空数组直接 resolve 和传入非 Promise 值用 Promise.resolve 包装。

三面挂在了工程化方面。面试官问了 Webpack 到 Vite 的迁移经验，我只说了体验更快但没给量化数据。问到构建产物的缓存策略（contenthash、splitChunks 配置），我答得模糊。最后问 CI/CD 流水线怎么做前端质量卡点（ESLint、单测覆盖率门禁、Lighthouse 性能评分），我基本没实践过。反思：前端不能只会写组件，工程化能力和数据意识同样重要。', 18, 560, '2025-10-22 19:40:00'),

(@a, '字节跳动', 'Java 后端', 5, 'offer', '字节跳动五轮面试总结：系统设计是决定性环节',
 '字节面试节奏非常快，共五轮，每轮 45~60 分钟。一面：Java 并发基础 + 算法题（两道 Medium LeetCode，给了 30 分钟）。重点考了 AQS 原理和 ReentrantLock 的公平/非公平模式区别。

二面：MySQL 索引原理深挖。从 B+ 树结构聊到联合索引最左前缀、覆盖索引消除回表、EXPLAIN type 列的含义。然后给了一条慢 SQL 让现场优化，我重建了联合索引顺序并加了覆盖索引字段，面试官满意。

三面是系统设计重头戏：设计一个类似微博 Feed 流系统，支持千万用户。我用了推拉结合方案——普通用户发帖推到粉丝收件箱（Push），大 V 发帖粉丝拉取时实时聚合（Pull），分界线是 5 万粉丝。存储层用 Redis SortedSet 按时间排序。面试官追问了冷启动和 Feed 去重的方案。这轮感觉是决定性环节——不懂分布式设计思维的很难过。

四面考项目深度和架构思考。五面 HR 聊价值观和抗压能力。全程两周内走完，整体体验是字节非常看重系统设计能力和思维的完整性，而不是背概念。', 47, 1180, '2025-11-15 21:25:00'),

(@z, '美团', '算法', 4, 'rejected', '美团算法岗面经：题目完成度高但沟通节奏失衡',
 '面试方向是到店搜索推荐。一面先聊了推荐系统的基本流程：召回（协同过滤 + 向量检索）、粗排（双塔模型）、精排（DeepFM/DIN）、重排（多样性打散）。然后让手推 AUC 的计算方式，以及为什么不能单纯用准确率评估 CTR 模型（样本不平衡，AUC 对阈值不敏感）。这部分答得还行。

二面上来两道算法题：一道是 LRU 缓存实现（HashMap + 双向链表），一道是给定用户点击序列预测下一个点击品类（我用了简单的 Markov Chain 思路，面试官引导用 Transformer 的 self-attention 建模）。代码都写完了，但过程中解释得太细导致超时，第二题没来得及讲时间复杂度分析。

三面是 case study：给了一个真实的外卖推荐场景——用户在搜索页搜了"炸鸡"，推荐列表应该展示什么？我分析了 query 理解（同义词扩展、品类映射）、个性化排序（历史偏好、地理位置、商家评分）、多目标优化（CTR + CVR + GPM）。面试官提到了实时特征的工程落地方案，我对 Flink 实时特征计算不够熟悉，回答得支支吾吾。

挂在了二面超时和三面工程化部分。教训：控制好表达节奏，先给结论再展开，不要沉浸在细节里。', 16, 510, '2025-12-08 18:00:00'),

(@l, '网易', 'Java 后端', 3, 'offer', '网易后端面试体验：注重业务理解与代码质量',
 '面试风格跟互联网大厂不太一样，更注重你对业务场景的理解和代码规范。一面基础部分问了 Spring Bean 生命周期的完整流程（实例化 → 属性注入 → Aware 回调 → BeanPostProcessor 前置 → InitializingBean → init-method → 后置处理 → 使用 → destroy）。然后问 MyBatis 一二级缓存的区别和失效条件，这块我答得很细。

二面围绕项目展开。我介绍了社区内容审核系统的设计：用户发帖 → MQ 异步投递 → 敏感词过滤（DFA 算法） → 图片 OCR 检测 → 人工复审队首页。面试官对 DFA 算法的实现很感兴趣，让我手写了构建 Trie 树和匹配的核心逻辑。还追问了如何处理拼音变体和火星文绕过敏感词——我提到了归一化预处理和正则增强。

三面比较特别：面试官让我当场做一次 Code Review。给了一段 200 行的 Spring Boot 代码，要求找出所有问题。我找到了：Controller 里直接写业务逻辑（违反分层）、事务范围过大（把 HTTP 调用包在 @Transactional 里）、日期用 Date 而不是 LocalDateTime、异常被空 catch 吞掉。面试官看起来很满意这种代码质量意识。两天后收到 offer。', 29, 760, '2025-12-28 20:30:00'),

(@a, '华为', '前端', 2, 'offer', '华为前端面试复盘：基础题稳定输出拿下 offer',
 '华为的面试流程是机试 + 两轮技术面 + 主管面。机试在牛客网上做，三道算法题（简单/中等/困难各一道），90 分钟内完成两道半拿到面试资格。

一面 45 分钟。先问了 CSS 布局：Flex 和 Grid 的区别和适用场景，如何实现一个两栏布局（左边固定宽度右边自适应）——我给了 Flex 和 Grid 两种方案。接着问了 JavaScript 原型链：手画了 __proto__ 和 prototype 的关系图，追问 Object.create(null) 和普通空对象的区别。然后是一道编程题：实现深拷贝，需要处理循环引用（用 WeakMap 记录已拷贝对象）。

二面偏项目和工程化。我讲了用 ECharts 做数据大屏的经验：动态数据刷新（WebSocket 推送 + requestAnimationFrame 渲染）、大数据量渲染优化（数据采样 + 增量更新而非全量重绘）、自适应屏幕分辨率（rem + scale 方案）。面试官追问了 Canvas 渲染和 SVG 渲染的性能差异——数据点过万时 Canvas 明显优于 SVG 因为不需要维护 DOM 节点。

主管面聊了职业规划、团队协作和加班态度。华为比较看重稳定性和执行力，技术深度要求没有一线互联网那么极端，但基础必须扎实。Offer 薪资在同级别中偏高。', 22, 640, '2026-01-15 17:05:00'),

(@z, '腾讯', 'Java 后端', 4, 'rejected', '腾讯后端二面挂点分析：系统设计缺少容量估算',
 '面的是微信支付相关团队。一面比较常规：JVM 内存模型（堆栈方法区的划分）、G1 和 ZGC 的区别、Spring AOP 实现原理（JDK 动态代理 vs CGLIB）。然后考了一道算法：合并 K 个有序链表，我用了最小堆方案，时间复杂度 O(NlogK)。

二面栽在了系统设计题上。题目是设计一个红包系统（类似微信红包）。我讲了拆红包算法（二倍均值法）、Redis 预生成金额列表、MySQL 记录领取流水的方案，整体逻辑没问题。但面试官追问：预估每秒多少请求量？需要多少台 Redis？数据库需要分多少个库？我完全没想过容量估算这件事，在那卡了好久。

具体来说：微信红包春节峰值每秒几十万次拆红包请求，单台 Redis 10 万 QPS，至少需要 5~10 台。数据库按用户 ID 取模分 128 张表，每张表千万级数据量。I/O 带宽、网络延迟、机器成本都要给出数量级的估算。

挂了之后的反思：系统设计不能停留在画架构图和选组件，必须带数字。以后准备系统设计题时，养成先做 back-of-envelope calculation（粗略估算）的习惯：DAU → 峰值 QPS → 单机能力 → 需要多少台 → 存储量 → 成本。', 19, 530, '2026-02-05 14:20:00'),

(@l, '字节跳动', '算法', 5, 'offer', '字节算法岗通关记录：刷题之外更看建模能力',
 '面试方向是推荐系统，共四轮技术面。一面基础+代码：先问了 Transformer 的 self-attention 计算公式（Q K V 矩阵乘法 + softmax + 缩放因子）、为什么要除以 sqrt(d_k)（防止点积过大导致 softmax 梯度消失）。代码题是实现 TopK 频率统计（HashMap 计数 + 最小堆）。

二面重点考了特征工程。给了一组用户行为数据（点击、收藏、购买序列），让设计推荐模型的输入特征。我从用户侧（年龄、地域、活跃度）、物品侧（品类、价格区间、CTR 历史）、交叉特征（用户对品类的历史偏好、时间衰减加权）三个维度展开。面试官追问了连续特征的离散化方案和 Embedding 维度选择策略。

三面是开放性设计题：冷启动场景下如何给新用户推荐内容？我分了三个阶段：注册时收集兴趣标签做基于内容的召回；前 3 天用 Bandit 策略（UCB / Thompson Sampling）做探索式推荐收集反馈；积累了 50+ 次行为后切换到个性化模型。面试官引导我对比了 Epsilon-Greedy 和 UCB 的优劣。

四面 HR。整体感觉字节算法面非常看重你对模型的直觉理解和工程落地经验，不会只考公式推导。建议准备 2~3 个端到端的建模案例，从数据处理到特征设计到模型选型到线上 A/B 测试全流程。', 41, 980, '2026-02-25 22:15:00'),

(@a, '美团', '前端', 3, 'rejected', '美团前端面试总结：性能优化有亮点但业务理解不足',
 '面的是外卖商家端前端团队。一面前端八股：浏览器渲染流程（构建 DOM → CSSOM → Render Tree → Layout → Paint → Composite）、回流和重绘的区别和触发条件、虚拟列表的实现原理（只渲染可视区域 + buffer zone 内的元素，滚动时动态替换）。代码题是封装一个防抖函数，要求支持 leading 和 trailing 模式。

二面深挖项目经验。我讲了一个复杂表单页面的性能优化：原来 200 个字段全量渲染导致首次挂载 800ms。优化方案是按 Tab 分区懒渲染 + v-if 控制非可见区域组件销毁 + 表单数据用 shallowRef 减少深层响应式追踪开销。优化后首次渲染降到 120ms。面试官对 shallowRef 的使用场景很感兴趣，追问了什么时候该用 reactive 什么时候用 shallowRef。

三面挂在了业务理解上。面试官问：商家端最核心的业务指标是什么？如何通过前端设计提升商家的接单效率？这些偏产品思维的问题我几乎没考虑过，只从技术角度回答了加载速度和交互流畅性。面试官期望听到的是：订单超时率、出餐时间提醒、异常订单高亮等业务场景驱动的设计思考。

教训：前端面试不只考技术，理解你做的产品服务于谁、核心指标是什么，同样重要。', 17, 500, '2026-03-10 19:55:00');

-- ═══════════════════════════════════════════════════════
--  沸点（12 条）：散点分布 2025-10 ~ 2026-03
-- ═══════════════════════════════════════════════════════
INSERT INTO moment (user_id, content, topic, like_count, created_at) VALUES
(@a, '今天把订单服务里的事务边界重新梳理了一遍，发现两个隐性自调用导致事务失效，修完之后回归一遍，终于没有脏数据告警了。', '今天学了什么', 23, '2025-10-10 08:10:00'),
(@z, '下午摸鱼看了半小时 JVM GC 日志分析视频，结果晚上排查线上抖动真用上了，摸鱼有时候也能变成投资。', '摸鱼日常', 17, '2025-10-28 15:20:00'),
(@l, '字节 offer 到手，最想分享的是复盘习惯：每场面试结束 30 分钟内写下问题、卡点、改进点，进步特别快。', 'offer晒单', 46, '2025-11-18 21:05:00'),
(@z, '今天被问到为什么缓存删除要做延迟双删，一紧张只回答了流程没讲风险，面试结束才想起来补偿策略没说。', '吐槽面试', 29, '2025-12-12 10:35:00'),
(@a, '推荐一个开源项目 OpenTelemetry Collector，最近接入后链路追踪清晰很多，定位慢接口真的省时间。', '开源推荐', 31, '2026-01-03 13:40:00'),
(@l, '上午学了 Go 的 context 取消传播，下午就把批处理任务改成可中断版本，线上超时任务终于可控。', '今天学了什么', 20, '2026-01-20 18:25:00'),
(@a, '原计划写文档，结果先给团队搭了个 Docker 本地一键环境，至少下周新人入组不用再被环境问题折磨。', '摸鱼日常', 15, '2026-02-03 09:15:00'),
(@z, '网易后端 offer +1，面试里最加分的是把线上故障复盘讲完整，建议大家准备一个真实案例。', 'offer晒单', 38, '2026-02-17 11:50:00'),
(@l, '面试官问我如何做容量估算，我只说了扩容没给数字，现场空气瞬间安静，提醒自己以后先给结论和量化。', '吐槽面试', 22, '2026-02-28 16:30:00'),
(@a, '强推一个前端监控开源方案：Sentry + 自研埋点网关，既能抓错误也能看业务路径，落地成本不高。', '开源推荐', 27, '2026-03-06 10:05:00'),
(@z, '今天把 MySQL 慢日志按接口维度做了聚合，终于定位到一个分页查询没走联合索引，性能提升立竿见影。', '今天学了什么', 24, '2026-03-11 14:55:00'),
(@l, '周五下午本来想摸鱼，结果被拉去做故障演练，虽然累但收获很大，第一次把限流降级预案完整跑通。', '摸鱼日常', 19, '2026-03-14 18:40:00');

