<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.mapper.CommentMapper">

  <sql id="BaseColumns">
    id,
    user_id,
    entity_type,
    entity_id,
    target_id,
    content,
    status,
    created_at,
    updated_at
  </sql>

  <select id="selectByEntity" resultType="com.echo.entity.Comment">
    SELECT
    <include refid="BaseColumns" />
    FROM comment
    WHERE entity_type = #{entityType}
      AND entity_id = #{entityId}
      AND status = 0
    ORDER BY created_at ASC, id ASC
    LIMIT #{offset}, #{limit}
  </select>

  <select id="selectCountByEntity" resultType="int">
    SELECT COUNT(1)
    FROM comment
    WHERE entity_type = #{entityType}
      AND entity_id = #{entityId}
      AND status = 0
  </select>

  <insert id="insert" parameterType="com.echo.entity.Comment" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO comment (
      user_id,
      entity_type,
      entity_id,
      target_id,
      content,
      status
    ) VALUES (
      #{userId},
      #{entityType},
      #{entityId},
      #{targetId},
      #{content},
      #{status}
    )
  </insert>

  <select id="selectById" resultType="com.echo.entity.Comment">
    SELECT
    <include refid="BaseColumns" />
    FROM comment
    WHERE id = #{id}
    LIMIT 1
  </select>
</mapper>
