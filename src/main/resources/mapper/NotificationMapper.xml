<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.mapper.NotificationMapper">

  <sql id="BaseColumns">
    id,
    user_id,
    type,
    entity_type,
    entity_id,
    from_user_id,
    status,
    created_at,
    updated_at
  </sql>

  <insert id="insert" parameterType="com.echo.entity.Notification" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO notification (
      user_id,
      type,
      entity_type,
      entity_id,
      from_user_id,
      status
    ) VALUES (
      #{userId},
      #{type},
      #{entityType},
      #{entityId},
      #{fromUserId},
      #{status}
    )
  </insert>

  <select id="selectByUser" resultType="com.echo.entity.Notification">
    SELECT
    <include refid="BaseColumns" />
    FROM notification
    WHERE user_id = #{userId}
    ORDER BY created_at DESC, id DESC
    LIMIT #{offset}, #{limit}
  </select>

  <select id="selectCountByUser" resultType="int">
    SELECT COUNT(1)
    FROM notification
    WHERE user_id = #{userId}
  </select>

  <select id="selectUnreadCount" resultType="int">
    SELECT COUNT(1)
    FROM notification
    WHERE user_id = #{userId}
      AND status = 0
  </select>

  <update id="markReadByIds">
    UPDATE notification
    SET status = 1,
        updated_at = NOW()
    WHERE user_id = #{userId}
      AND status = 0
      AND id IN
      <foreach collection="ids" item="id" open="(" separator="," close=")">
        #{id}
      </foreach>
  </update>

  <update id="markReadAll">
    UPDATE notification
    SET status = 1,
        updated_at = NOW()
    WHERE user_id = #{userId}
      AND status = 0
  </update>
</mapper>
