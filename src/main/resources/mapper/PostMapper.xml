<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.mapper.PostMapper">

  <sql id="BaseColumns">
    id,
    user_id,
    title,
    content,
    type,
    status,
    comment_count,
    like_count,
    score,
    created_at,
    updated_at
  </sql>

  <insert id="insert" parameterType="com.echo.entity.Post" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO post (
      user_id,
      title,
      content,
      type,
      status,
      comment_count,
      like_count,
      score
    ) VALUES (
      #{userId},
      #{title},
      #{content},
      #{type},
      #{status},
      #{commentCount},
      #{likeCount},
      #{score}
    )
  </insert>

  <select id="selectById" resultType="com.echo.entity.Post">
    SELECT
    <include refid="BaseColumns" />
    FROM post
    WHERE id = #{id}
    LIMIT 1
  </select>

  <select id="selectPosts" resultType="com.echo.entity.Post">
    SELECT
    <include refid="BaseColumns" />
    FROM post
    <where>
      status != 2
      <if test="userId != null">
        AND user_id = #{userId}
      </if>
    </where>
    <choose>
      <when test="orderBy == 'hot'">
        ORDER BY score DESC, created_at DESC, id DESC
      </when>
      <otherwise>
        ORDER BY created_at DESC, id DESC
      </otherwise>
    </choose>
    LIMIT #{offset}, #{limit}
  </select>

  <select id="selectPostCount" resultType="int">
    SELECT COUNT(1)
    FROM post
    <where>
      status != 2
      <if test="userId != null">
        AND user_id = #{userId}
      </if>
    </where>
  </select>

  <update id="updateType">
    UPDATE post
    SET type = #{type},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateStatus">
    UPDATE post
    SET status = #{status},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateCommentCount">
    UPDATE post
    SET comment_count = #{commentCount},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateLikeCount">
    UPDATE post
    SET like_count = #{likeCount},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateScore">
    UPDATE post
    SET score = #{score},
        updated_at = NOW()
    WHERE id = #{id}
  </update>
</mapper>
