<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.mapper.ColumnArticleMapper">

  <sql id="BaseColumns">
    id,
    user_id,
    column_name,
    title,
    content,
    cover_img,
    status,
    like_count,
    view_count,
    created_at,
    updated_at
  </sql>

  <select id="selectById" resultType="com.echo.entity.ColumnArticle">
    SELECT
    <include refid="BaseColumns" />
    FROM column_article
    WHERE id = #{id}
      AND status = 0
    LIMIT 1
  </select>

  <select id="selectList" resultType="com.echo.entity.ColumnArticle">
    SELECT
    <include refid="BaseColumns" />
    FROM column_article
    <where>
      status = 0
      <if test="columnName != null and columnName != ''">
        AND column_name = #{columnName}
      </if>
    </where>
    ORDER BY created_at DESC, id DESC
    LIMIT #{offset}, #{limit}
  </select>

  <select id="selectCount" resultType="int">
    SELECT COUNT(1)
    FROM column_article
    <where>
      status = 0
      <if test="columnName != null and columnName != ''">
        AND column_name = #{columnName}
      </if>
    </where>
  </select>
</mapper>
