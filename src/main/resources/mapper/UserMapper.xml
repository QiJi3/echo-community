<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.mapper.UserMapper">

  <sql id="BaseColumns">
    id,
    username,
    password,
    email,
    avatar,
    role,
    status,
    created_at,
    updated_at
  </sql>

  <select id="selectByUsername" resultType="com.echo.entity.User">
    SELECT
    <include refid="BaseColumns" />
    FROM `user`
    WHERE username = #{username}
    LIMIT 1
  </select>

  <select id="selectById" resultType="com.echo.entity.User">
    SELECT
    <include refid="BaseColumns" />
    FROM `user`
    WHERE id = #{id}
    LIMIT 1
  </select>

  <insert id="insert" parameterType="com.echo.entity.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO `user` (
      username,
      password,
      email,
      avatar,
      role,
      status
    ) VALUES (
      #{username},
      #{password},
      #{email},
      #{avatar},
      #{role},
      #{status}
    )
  </insert>

  <update id="updatePassword">
    UPDATE `user`
    SET password = #{password},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateRole">
    UPDATE `user`
    SET role = #{role},
        updated_at = NOW()
    WHERE id = #{id}
  </update>
</mapper>
